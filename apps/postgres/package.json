{
  "name": "@repo/postgres",
  "version": "0.0.0",
  "description": "Testcontainer postgres module",
  "main": "src/index.ts",
  "scripts": {
    "build": "tsc --outDir ./dist",
    "test": "echo \"Message: no test for this package\" && exit 0",
    "dev:postgres": "node ./dist/main.js"
  },
  "private": true,
  "dependencies": {
    "@testcontainers/postgresql": "^10.8.2"
  },
  "devDependencies": {
    "typescript": "^5.4.5"
  }
}
