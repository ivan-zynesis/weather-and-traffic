{
  "name": "@repo/postgres",
  "version": "0.0.0",
  "description": "Testcontainer postgres module",
  "main": "src/index.ts",
  "scripts": {
    "build": "tsc",
    "test": "echo \"Message: no test for this package\" && exit 0"
  },
  "private": true,
  "dependencies": {
    "@testcontainers/postgresql": "^10.8.2"
  },
  "devDependencies": {
    "typescript": "^5.4.5",
    "ts-node": "^10.9.2"
  }
}
